https://www.codewars.com/kata/5287e858c6b5a9678200083c/train/python

def narcissistic( value ):
    s = 0
    input = value
    power = len(str(value))
    while input > 0:
        s += (input % 10)** power
        input = input // 10
    return s == value
